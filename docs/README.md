# Docs / 文档

This folder explains the modeling framework, case settings, parameters, figures, code sections, and reproduction process.

本文件夹用于说明模型框架、算例设置、参数含义、图像输出、代码段落和复现流程。

- `terminology.md`: terminology for `ecv` and `eal` / `ecv` 与 `eal` 的术语说明。

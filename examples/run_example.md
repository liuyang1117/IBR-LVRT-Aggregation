# Run Example / 运行示例

Open MATLAB in the repository root directory.

在仓库根目录打开 MATLAB。

```matlab
addpath('matlab');
photovoltaic_case;
wind_farm_case;
```

To run both using one script:

也可以用一个脚本运行两个算例：

```matlab
addpath('matlab');
run_all_cases;
```

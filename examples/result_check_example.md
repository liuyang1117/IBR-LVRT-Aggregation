# Result Check Example / 结果检查示例

After running a case, use:

运行算例后，可使用：

```matlab
whos
```

to check generated workspace variables.

检查生成的 MATLAB 工作区变量。

Important updated variables:

重要更新变量：

```text
ecv_plot
eal_plot
ecv_piecewise_approx_plot
eal_piecewise_approx_plot
```
